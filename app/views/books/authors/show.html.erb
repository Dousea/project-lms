<p>
  <strong>Name:</strong>
  <%= @author.name %>
</p>

<p>
  <strong>Books:</strong>
  <ul>
    <% @author.books.each do |book| %>
      <li><%= link_to book.title, book_path(book) %></li>
    <% end %>
  </ul>
</p>

<%= link_to 'Back', books_authors_path %>