<h1>Listing Subjects</h1>
 
<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
 
  <% @subjects.each do |subject| %>
    <tr>
      <td><%= subject.name %></td>
      <td><%= link_to 'Show', books_subject_path(subject) %></td>
    </tr>
  <% end %>
</table>

<%= paginate @subjects %>