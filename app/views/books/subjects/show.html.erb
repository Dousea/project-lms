<p>
  <strong>Name:</strong>
  <%= @subject.name %>
</p>

<p>
  <strong>Books:</strong>
  <ul>
    <% @subject.books.each do |book| %>
      <li><%= link_to book.title, book_path(book) %></li>
    <% end %>
  </ul>
</p>

<%= link_to 'Back', books_subjects_path %>